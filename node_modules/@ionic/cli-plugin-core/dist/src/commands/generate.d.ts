import { CommandLineInputs, CommandLineOptions, Command, ValidationError } from '@ionic/cli-utils';
export declare class GenerateCommand extends Command {
    run(inputs: CommandLineInputs, options: CommandLineOptions, validationErrors: ValidationError[]): Promise<void>;
}
