import { Command, CommandLineInputs, CommandLineOptions } from '@ionic/cli-utils';
/**
 * Metadata about the docs command
 */
export declare class UploadCommand extends Command {
    private createZipStream();
    private requestSnapshotUpload(token);
    private uploadSnapshot(snapshot, zip, progress);
    run(inputs: CommandLineInputs, options: CommandLineOptions): Promise<void>;
}
